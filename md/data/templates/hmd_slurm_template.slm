#!/bin/bash
#SBATCH -J $jobname
#SBATCH -e $jobname.e
#SBATCH -o $jobname.o
#SBATCH -p $queue
#SBATCH $tasktype
#SBATCH --time=6-0:00

export HOOMD_WALLTIME_STOP=$((`date +%s` + 144 * 3600 - 10 * 60))

module load conda/3
conda activate $activate

$command
